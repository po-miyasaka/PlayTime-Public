# Uncomment the next line to define a global platform for your project
# platform :ios, '11.0'

target 'PlayTime' do
  use_frameworks!
  pod 'SwiftLint'


  target 'PlayTimeTests' do
    inherit! :search_paths
    pod 'Quick'
    pod 'Nimble'
  end

end
